'''Жешко Даниил ИУ7-15Б'''

'''Написать программу, которая по введенным целочисленным
координатам трех точек на плоскости вычисляет длины сторон
образованного треугольника и длину медианы, проведенной из
наибольшего угла.
Определить, является ли треугольник прямоугольным.
Далее вводятся координаты точки. Определить, находится ли
точка внутри треугольника. Если да, то найти расстояние от точки
до ближайшей стороны треугольника.
'''
from math import sqrt

# Ввод координатных точек треугольника
x1, y1 = map(int, input("Введите координаты точки a: ").split())
x2, y2 = map(int, input("Введите координаты точки b: ").split())
x3, y3 = map(int, input("Введите координаты точки c: ").split())

# Вычисляем длины сторон треугольника
a = sqrt((x1 - x2) ** 2 + (y1 - y2) ** 2)
b = sqrt((x2 - x3) ** 2 + (y2 - y3) ** 2)
c = sqrt((x1 - x3) ** 2 + (y1 - y3) ** 2)

#проверка на треугольник
if a + b > c and a + c > b and b + c > a:
    print("Треугольник существует")

    # Выводим длины сторон
    print("Длина a: {:.5g}".format(a))
    print("Длина b: {:.5g}".format(b))
    print("Длина c: {:.5g}".format(c))

    # Находим из найденых сторон минимальную,максимальную и среднюю стороны
    max_side = max(a, b, c)
    min_side = min(a, b, c)
    centr_side = (a + b + c) - min_side - max_side

    # Вычисляем медиану из большего угла
    mediana = sqrt(2 * centr_side ** 2 + 2 * min_side ** 2 - max_side ** 2) / 2

    # Выводи значение медианы
    print("Длина медианы: {:.5g}".format(mediana))

    # Находим вид треугольника(прямоугольный,не прямоугольный)
    if c == sqrt(a ** 2 + b ** 2) or a == sqrt(c ** 2 + b ** 2) or b == sqrt(a ** 2 + c ** 2):
        print("Прямоугольный")
    else:
        print("Не прямоугольный")

    # Ввод координатных значений точки на проверку (лежит ли она в треугольнике,лежит на стороне,не лежит в треугольнике)
    x, y = map(int, input("Введите координаты проверяемой точки: ").split())

    # Формула нахождения коэффициентов местоположения точки (лежит ли она в треугольнике,лежит на стороне,не лежит в треугольнике)
    t1 = (x1 - x) * (y2 - y1) - (x2 - x1) * (y1 - y)
    t2 = (x2 - x) * (y3 - y2) - (x3 - x2) * (y2 - y)
    t3 = (x3 - x) * (y1 - y3) - (x1 - x3) * (y3 - y)

    # Вывод точки (лежит ли она в треугольнике,лежит на стороне,не лежит в треугольнике)
    if (t1 > 0 and t2 > 0 and t3 > 0) or (t1 < 0 and t2 < 0 and t3 < 0):
        print("Лежит в треугольнике")
        # Нахождение длины от заданй точки до сторон
        ad = sqrt((x - x1) ** 2 + (y - y1) ** 2)
        bd = sqrt((x - x2) ** 2 + (y - y2) ** 2)
        cd = sqrt((x - x3) ** 2 + (y - y3) ** 2)

        # Нахождение полупериметра образованных треугольников
        p1 = (ad + bd + a) / 2
        p2 = (bd + cd + b) / 2
        p3 = (cd + ad + c) / 2

        # Находим значение минимального расстояния от точки до ближайшей прямой треугольника
        height_1 = (2 / a) * sqrt(p1 * (p1 - a) * (p1 - ad) * (p1 - bd))
        height_2 = (2 / b) * sqrt(p2 * (p2 - b) * (p2 - bd) * (p2 - cd))
        height_3 = (2 / c) * sqrt(p3 * (p3 - c) * (p3 - cd) * (p3 - ad))
        min_height = min(height_1, height_2, height_3)

        # Выводим расстояние
        print("Расстояние от точки до треугольника равно: {:.5g}".format(min_height))

    elif t1 == 0 or t2 == 0 or t3 == 0:
        print("Лежит на стороне треугольника")
        print("Расстояние от точки до треугольника равно:0")
    else:
        print("Не лежит в треугольнике")
else:
    print("Треугольник не существует")