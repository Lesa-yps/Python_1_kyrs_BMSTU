print - это функция
print(object1, object2, ..., sep=' ', end='\n', file=sys.stdout, flush=False)
*objects — объект/объекты которые необходимо вывести;
sep — разделитель между объектами. В качестве своего значения можно передавать строку или None (по умолчанию пробел " ");
end — символ в конце строки (по умолчанию перенос строки \n);
file — file-like объект [поток] (по умолчанию sys.stdout);
flush — принудительный сброс потока (выводимая строка может какое-то время сохраняться и не выводиться)[работает с версии Python 3.3] (по умолчанию False).

ФОРМАТИРОВАНИЕ:
https://pythonru.com/osnovy/formatirovanie-v-python-s-pomoshhju-format
'{[имя]:[чем заполнить пробелы][куда сдвинуть][минимум места под всё число][разделитель разрядов][.максимум места под знаки после запятой][формат]}'.format(имя)

!!!

print('{:*<12_.2f}'.format(235235.3493))
235_235.35**

*Стандартные потоки ввода-вывода
0 - stdin
1 - stdout
2 - stderr